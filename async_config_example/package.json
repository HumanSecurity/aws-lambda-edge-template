{
  "type": "module",
  "name": "aws-lambda-edge-enforcer-template-async-config",
  "version": "1.0.0",
  "description": "js service for testing enforcers",
  "main": "index.js",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "node scripts/build.cjs",
    "zip": "node scripts/package.cjs",
    "prebuild": "npm run clean",
    "prezip": "npm run build"
  },
  "license": "ISC",
  "dependencies": {
    "@humansecurity/aws-lambda-edge-enforcer": "^4.10.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.130",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.11.25",
    "adm-zip": "^0.5.16",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.20.0",
    "ts-node": "^10.9.2",
    "typescript": "^4.9.4"
  }
}
